// ============================================================================
//  uBoost Runner — единый конфиг. Меняй тут — НЕ лезь в логику.
// ============================================================================

export const CONFIG = {
  // --- Ссылки (ПЛЕЙСХОЛДЕРЫ / реальные) -------------------------------------
  STORE_URL: 'https://uboo.st/',           // куда ведёт CTA «настоящий Юбуст»
  GAME_URL: 'https://deprav1.github.io/uboost-runner/',  // ссылка на игру для шеринга (GitHub Pages)
  TG_BOT_URL: 'https://uboo.st/',          // опц. ссылка на бота

  // --- Полосы / геометрия ---------------------------------------------------
  LANES: 3,
  LANE_BAND_TOP: 0.30,     // верх игровой полосы (доля высоты)
  LANE_BAND_BOTTOM: 0.84,  // низ игровой полосы
  PLAYER_X: 0.20,          // X ракеты (доля ширины)

  // --- Скорость / прогрессия (в CSS-пикселях в секунду) ---------------------
  BASE_SPEED: 360,
  SPEED_GROWTH: 7,         // прибавка скорости за каждые 100 «метров»
  MAX_SPEED: 1100,
  BOOST_SPEED: 1500,
  LANE_TWEEN: 0.018,       // скорость перелёта между полосами (мс^-1, выше = резче)

  // --- Спавн ----------------------------------------------------------------
  SPAWN_GAP_START: 1.15,   // секунд между волнами в начале
  SPAWN_GAP_MIN: 0.42,     // минимум на хардкоре
  SPAWN_GAP_DECAY: 0.0009, // насколько ужимается за «метр»
  BOOST_EVERY: 11,         // примерно каждые N волн — шанс на VPN-буст
  BOOST_CHANCE: 0.55,

  // --- Буст ------------------------------------------------------------------
  BOOST_DURATION: 5.0,     // секунд неуязвимости/гипер-скорости

  // --- Очки ------------------------------------------------------------------
  SCORE_PER_METER: 0.6,
  SCORE_PER_DODGE: 12,
  SCORE_NEAR_MISS: 25,
  SCORE_SMASH: 40,         // разбитое в бусте препятствие

  // --- Палитра синтвейв ------------------------------------------------------
  COLORS: {
    bgTop: '#1a0b2e',
    bgBottom: '#05010f',
    sunTop: '#ff2d95',
    sunBottom: '#ffd166',
    grid: '#ff2d95',
    gridFar: '#7b2ff7',
    cyan: '#15f5ff',
    magenta: '#ff2d95',
    purple: '#9d4edd',
    yellow: '#ffe14d',
    danger: '#ff3b3b',
    rocket: '#15f5ff',
    rocketGlow: '#ff2d95',
    text: '#eafcff',
  },

  // --- Прочее ----------------------------------------------------------------
  AUDIO_DEFAULT_ON: true,
  STORAGE_KEY: 'uboost_runner_v1',
};
